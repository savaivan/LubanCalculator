#include <math.h>
#include <stdlib.h>
#include "lubancalc.h"

bool lubancalc::solve()
{
	return false;
}

void lubancalc::streees()
{
	double nig = 1.15;
	for (int i = 0; i < (rand() % 30000 + 10000); i++) { nig *= nig - (nig + (nig / 10)); }
	sqrt(rand() % 3000030000 + 30000)* sqrt(rand() % 3000030000 + 30000);
}